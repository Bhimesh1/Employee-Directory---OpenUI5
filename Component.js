sap.ui.define([
  "sap/ui/core/UIComponent"
], function (UIComponent) {
  "use strict";

  return UIComponent.extend("employee.directory.Component", {
    metadata: {
      manifest: "json"
    }
  });
});